server:
  port: 8080
spring:
  # datasource配置
  datasource:
    name: blog
    type: com.zaxxer.hikari.HikariDataSource  # 使用 Hikari 数据源
    url: jdbc:mysql://localhost:3306/blog?useUnicode=true&characterEncoding=UTF-8&allowMultiQueries=true&useSSL=true
    username: root
    password: 43997K7k.

    # 数据库连接池配置(Hikari)
    hikari:
      connection-timeout: 30000
      maximum-pool-size: 10
      minimum-idle: 1
      idle-timeout: 600000
      pool-name: HikariCP
      max-lifetime: 1800000
      connection-test-query: SELECT 1

  #jpa/hibernate相关配置
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
        # 禁用OpenSessionInView
        open-in-view: false
        audit_table_prefix: fs_ # 审计表前缀
        audit_table_suffix: _aud # 审计表后缀
        revision_field_name: rev # 版本字段名
        revision_type_field_name: rev_type # 版本类型字段名
        store_data_at_delete: true # 删除时是否存储数据

    jackson:
      serialization:
        fail-on-empty-beans: false

  data:
    jpa:
      repositories:
        enabled: true
    jdbc:
      repositories:
        enabled: false
    redis:
      host: 127.0.0.1
      port: 6379
      password:  # 如果有密码，请设置
      database: 0
      timeout: 10000
      lettuce:
        pool:
          max-active: 8   # 连接池最大连接数
          max-wait: -1    # 连接池最大阻塞等待时间（使用负值表示没有限制）
          max-idle: 8     # 连接池中的最大空闲连接
          min-idle: 0     # 连接池中的最小空闲连接

  flowable:
    database-schema-update: true
    db-history-used: true
    history-level: full
    async-executor-activate: false
    process-definition-location-prefix: classpath:/processes/
    check-process-definitions: true
    # 字体配置
    font:
      activity: 宋体
      label: 宋体
      annotation: 宋体s/

  #    #druid相关配置
  #    druid:
  #      #监控统计拦截的filters
  #      filters: stat
  #      driver-class-name: com.mysql.jdbc.Driver
  #      #基本属性
  #
  #      #配置初始化大小/最小/最大
  #      initial-size: 1
  #      min-idle: 1
  #      max-active: 20
  #      #获取连接等待超时时间
  #      max-wait: 60000
  #      #间隔多久进行一次检测，检测需要关闭的空闲连接
  #      time-between-eviction-runs-millis: 60000
  #      #一个连接在池中最小生存的时间
  #      min-evictable-idle-time-millis: 300000
  #      validation-query: SELECT 'x'
  #      test-while-idle: true
  #      test-on-borrow: false
  #      test-on-return: false
  #      #打开PSCache，并指定每个连接上PSCache的大小。oracle设为true，mysql设为false。分库分表较多推荐设置为false
  #      pool-prepared-statements: false
  #      max-pool-prepared-statement-per-connection-size: 20

  aop:
    auto: true
    proxy-target-class: true

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 100MB

  mail:
    host: smtp.qq.com
    port: 587
    username: 1448185924@qq.com
    password: czlrpanbmcerihga
    properties:
      mail:
        transport:
          protocol: smtp
        smtp:
          auth: true
          starttls:
            enable: true
          ssl:
            trust: smtp.qq.com
        debug: true
  main:
    allow-bean-definition-overriding: true

logging:
  file:
    name: logs/blog.log
  level:
    root: INFO
    com.kitty.blog: DEBUG

#mybatis:
#  mapper-locations: classpath:mapper/*.xml
#  type-aliases-package: com.kitty.model

swagger:
  show: true
#分页
pagehelper:
  helper-dialect: mysql
  reasonable: true
  support-methods-arguments: true
  params: count=countSql
  retrunPageInfo: check

aliyun:
  oss:
    endpoint: "oss-cn-beijing.aliyuncs.com"
    access-key-id: "LTAI5tSejtaTDzN3S2cpuWYh"
    access-key-secret: "nzdIdnvKrzgSq1dh31JNar0p3pJCgD"
    bucket-name: "hello-kitty-cat-sweet"

# 百度云内容审核配置
baidu:
  content:
    appId: "6620094" # 替换为你的 App ID
    apiKey: "tZvDGGlR83jubJsuksqnYObj" # 替换为你的 API Key
    secretKey: "7qZr8Y1lTzm6wEUteYU8u6W4KXKZkctL" # 替换为你的 Secret Key

jwt:
  secret: "gZYAaBzjFS6R6aLU9uTlIZb2vzsnxKv4KE38H2H6X3c="
  expiration: 3600 # 1小时过期

frontend:
  url:
    http://localhost:5173

encryption:
  key:
    pf46F20xY/x+owVHLxbSeA==